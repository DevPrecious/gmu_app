import 'package:gmu_app/constants/api_constants.dart';

class EndpointConstants {
  static Uri registerEndpoint = Uri.parse('${ApiConstants.endpoint}/register');
  static Uri loginEndpoint = Uri.parse('${ApiConstants.endpoint}/login');
}
